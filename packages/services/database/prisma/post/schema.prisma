generator client {
  provider = "prisma-client"
  output = env("GENERATE_OUTPUT")
}

datasource db {
  provider = "postgresql"
}

model Post {
  id    Int     @id @default(autoincrement())
  title String
  content  String?
}
